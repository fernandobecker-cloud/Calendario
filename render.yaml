services:
  - type: web
    name: crm-campaign-planner
    env: python
    autoDeploy: true
    buildCommand: pip install -r requirements.txt
    startCommand: bash start.sh
    healthCheckPath: /health
